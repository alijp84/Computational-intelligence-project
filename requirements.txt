# MetaMind CI Framework - Requirements
# Compatible with Python 3.8+ (recommended: Python 3.9-3.11)

# Core Scientific Computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<1.14.0

# Data Handling & Machine Learning
pandas>=1.3.0,<2.3.0
scikit-learn>=1.0.0,<1.5.0

# LLM Integration (required for real LLM mode; mock mode works without)
openai>=1.0.0,<2.0.0

# Visualization (optional but recommended for reports)
matplotlib>=3.5.0,<3.10.0
seaborn>=0.11.0,<0.14.0

# Development & Testing
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# Logging & Utilities
python-dateutil>=2.8.0,<3.0.0

# Environment Management (optional but recommended)
python-dotenv>=1.0.0,<2.0.0

# Version Notes:
# - numpy<2.0.0: Ensures compatibility with scikit-learn 1.x series
# - scikit-learn<1.5.0: Maintains API stability for academic reproducibility
# - openai<2.0.0: Avoids breaking changes in newer API versions
# - matplotlib<3.10.0: Prevents rendering issues in headless environments
#
# Installation:
#   pip install -r requirements.txt
#
# For development environments with visualization:
#   pip install -r requirements.txt matplotlib seaborn
#
# For production/headless servers (no visualization):
#   pip install -r requirements.txt --no-deps matplotlib  # Skip matplotlib dependencies
#
# Note: Mock LLM mode (default for development) works WITHOUT openai package.
#       Real LLM mode requires OPENAI_API_KEY environment variable.